#include "main.h"
/**
 * _abs - returns its absolute value
 * @n: input
 *
 * Return: n absolute value
 * otherwise return 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
